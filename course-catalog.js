window.addEventListener('youssef-auth-ready',async(e)=>{
 const P=window.YoussefAlaaPlatform;if(!P)return;const host=document.querySelector('#dynamic-course-catalog');if(!host)return;const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));const money=n=>new Intl.NumberFormat('ar-EG').format(Number(n||0));
 try{const s=await P.getDocs(P.query(P.collection(P.db,'courses'),P.where('published','==',true),P.limit(100)));const rows=s.docs.map(d=>({id:d.id,...d.data()}));host.innerHTML=rows.map(c=>`<article class="platform-panel"><span class="platform-badge success">متاح</span><h3>${esc(c.title)}</h3><p style="color:#5b6072">${esc(c.description||'')}</p><div style="display:flex;justify-content:space-between;align-items:center;gap:8px;flex-wrap:wrap"><b>${money(c.price)} ج.م</b><div class="platform-actions"><button class="platform-btn primary" data-add-cart="${c.id}">إضافة للسلة</button><a class="platform-btn" href="/payment.html?courseId=${encodeURIComponent(c.id)}">الدفع الآن</a></div></div></article>`).join('')||'<div class="platform-empty">سيتم إضافة الكورسات المنشورة هنا قريبًا.</div>';host.querySelectorAll('[data-add-cart]').forEach(b=>b.onclick=()=>{const cart=JSON.parse(localStorage.getItem('ysa_cart')||'[]');if(!cart.includes(b.dataset.addCart))cart.push(b.dataset.addCart);localStorage.setItem('ysa_cart',JSON.stringify(cart));P.messageBox('تمت إضافة الكورس إلى السلة.','success')})}catch(err){host.innerHTML=`<div class="platform-empty">تعذر تحميل الكورسات.</div>`}
});
